services:
  - type: web
    name: profitwave-bridge
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bridge_cloud.py
    envVars:
      - key: PORT
        value: 8080
      - key: PYTHON_VERSION
        value: 3.10.0
