services:
  - type: web
    name: profitwave-bridge
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bridge_cloud:app --host 0.0.0.0 --port 8080
    envVars:
      - key: PORT
        value: 8080
      - key: PYTHON_VERSION
        value: 3.10.0
